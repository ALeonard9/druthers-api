Implemented and committed the multi-stage Docker build.

- Commit: `042f864 chore: convert api dockerfile to a multi-stage build`
- Runtime keeps only built wheels, app code, and `libpq`; builder retains gcc/Rust/Cargo/PostgreSQL headers.
- Healthcheck now targets `/health`.
- Added Dockerfile regression tests.
- `pytest -q`: `874 passed, 17 warnings`.

`WORKER_REPORT.md` is updated but not committed. Docker boot and image-size verification were not run per fleet-worker boundaries; demo steps are in the report.
